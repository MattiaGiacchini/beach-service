version: "1"
services:
  beach-service-frontend:
    image: beach-service-frontend:latest
    environment:
      PORT: 8080
    ports:
      - 8080:8080
  beach-service-backend:
    image: beach-service-backend:latest
    environment:
      PORT: 3000
    ports:
      - 3000:3000
